spring:
  application:
    name: AUTHORIZATION-SERVER
  cloud:
    config:
      enabled: true
      uri: http://${HOST_IP:localhost}:9296/config-server
      name: account-service,${spring.application.name}