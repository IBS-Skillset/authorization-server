---
apiVersion: v1
kind: Service
metadata:
  name: authserver
  namespace: beta-app
spec:
  type: ClusterIP
  ports:
    - port: 9000
      targetPort: 9000
  selector:
    app.kubernetes.io/name: authserver